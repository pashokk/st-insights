# Anthropic — required (get from console.anthropic.com)
ANTHROPIC_API_KEY=your-anthropic-key-here

# SensorTower key is passed in at runtime via the UI (not stored here)
# If you prefer to hardcode it server-side, add it here and read from process.env in the route
# SENSORTOWER_API_KEY=your-st-key-here
